# <release name or version>

Written for a user of the product, not a developer of it. No module names, no
internal vocabulary, nothing that assumes the reader has seen the code.

## What is new

<Lead with what a person can now do that they could not before. One short
paragraph per item, in the order that matters to them rather than the order the
work happened.>

## What is better

<Improvements to things that already worked. Say the difference in terms the
user feels: faster to load, fewer steps, clearer error.>

## What is fixed

<The symptom, described the way a user would describe it. If they never saw it,
it does not belong in a release note.>

## What you need to do

<Any action required: update a setting, re authenticate, migrate data. Put this
last so it is the thing left on screen, and write "nothing" when nothing is
required.>
